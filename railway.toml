[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm run build && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthchecks = { enabled = true, interval = 10 }
